[project]
name = "copy-anything"
version = "0.1.0"
description = "把网站、GitHub 项目和 Skill 转换为可理解、可复用、可重建的 DNA"
requires-python = ">=3.11"

[project.scripts]
copy-anything = "copy_anything.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
